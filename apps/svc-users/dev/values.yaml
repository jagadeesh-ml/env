# === base defaults (needed by the chart) ===
image:
  repository: ghcr.io/Jagadeesh-ml/svc-users
  tag: "sha-placeholder"
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 8080

replicaCount: 2

# give resources a unique name so they don't clash with other apps
nameOverride: svc-users
